import 'package:flutter/material.dart';

class PhotoGalleryHomePage extends StatelessWidget {
  const PhotoGalleryHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is the gallery"),
    );
  }
}
